It should be clear that a kernel is distinctively distrustful of processes, but does a process have to trust the kernel? 
If so, what is it expecting from the kernel?
    - Yes a process has to trust the kernel. Specifically, it has to trust the kernel to isolate/protect it frmo other processes so it's data does not get corrupted